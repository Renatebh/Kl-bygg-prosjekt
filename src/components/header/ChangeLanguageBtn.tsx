import styles from "./styles/navlinks.module.css";

const ChangeLanguageBtn = () => {
  return (
    <>
      <button className={styles["changeLanguage-btn"]}>NO</button>
    </>
  );
};

export default ChangeLanguageBtn;
